{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "ensure-dev-services",
      "type": "shell",
      "command": "docker compose -f docker-compose.dev.yml up -d redis postgres",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      },
      "problemMatcher": []
    }
  ]
}
